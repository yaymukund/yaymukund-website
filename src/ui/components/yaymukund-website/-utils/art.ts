export interface ArtMetadata {
  name: string,
  ext: string,
  caption: string,
}

const art: ArtMetadata[] = [
  { name: "actor", ext: "jpg", caption: "painting of some famous actor" }
];

export default art;
