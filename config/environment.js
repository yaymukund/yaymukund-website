'use strict';

module.exports = function(environment) {
  let ENV = {
    rootURL: '/',
    modulePrefix: 'yaymukund-website',
    artDir: 'assets/images/art/',
    environment: environment,
    locationType: 'auto',
  };

  return ENV;
};
